import localFont from "next/font/local";

export const tungstenBold = localFont({
  src: "./Tungsten-Bold.woff",
  variable: "--tungsten-bold",
  weight: "700",
});

export const tungstenSemiBold = localFont({
  src: "./Tungsten-Semibold.woff",
  variable: "--tungsten-semi-bold",
  weight: "400",
});
